这是一个测试文章。我们将使用F5-TTS来生成语音。

这个系统可以将长篇文章切割成短句，然后为每个句子生成高质量的语音。

系统会自动处理标点符号，确保每个句子的长度适合语音合成。

你可以使用不同的音色文件来生成不同风格的语音。

This is a test article. We will use F5-TTS to generate speech.

The system can split long articles into short sentences and generate high-quality audio for each sentence.

The system automatically handles punctuation to ensure each sentence is suitable for speech synthesis.
